apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: person-server
  namespace: aauth
spec:
  parentRefs:
    - { name: edge, namespace: uma-edge }
  hostnames: ["ps.uma.lab"]
  rules:
    - backendRefs:
        - { name: person-server, port: 8765 }
