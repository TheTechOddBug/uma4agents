apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: agent-operator
  namespace: sterling-vance
spec:
  parentRefs:
    - { name: edge, namespace: uma-edge }
  hostnames: ["agent.uma.lab"]
  rules:
    - backendRefs:
        - { name: agent-operator, port: 9030 }
