profile: ambient
# One per node, in the path of every meshed connection, so its footprint is
# the one that matters most on a three-node laptop cluster.
resources:
  requests: { cpu: 50m, memory: 128Mi }
  limits: { memory: 256Mi }
